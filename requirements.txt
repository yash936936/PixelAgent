# Fix for a gap flagged in review: every dependency here used to be pinned
# with only a lower bound (">="), which let an untested newer release get
# installed silently -- this project actually hit a real ImportError from
# an ambiguous google-genai install during this exact hardening pass. Every
# version below is one that has been installed and run against this
# codebase's test suite (160 passing tests) in this environment. Pin exact
# versions with `==`; if you need a newer release, bump it deliberately and
# re-run the full test suite first.
google-genai==2.11.0
playwright==1.56.0
python-dotenv==1.2.2
pydantic==2.13.4
pytesseract==0.3.13
pillow==12.1.1
# pyautogui requires a real display/X server and could not be installed or
# exercised in the sandboxed environment this project was hardened in --
# this version is a reasonable recent stable release, NOT one that has been
# verified against this codebase. Verify it yourself on your target machine
# (Windows) before relying on src/action/mouse_keyboard.py in production.
pyautogui==0.9.54

# Windows-only, for src/security/at_rest.py's encryption-at-rest of episodic/semantic memory
# (docs/DECISIONS.md 2026-08-02, Phase 8). Not installable in this project's Linux build/test
# environment -- src/security/at_rest.py detects its absence and degrades to plaintext storage
# with a loud warning rather than failing. Install this on your real Windows machine to enable
# encryption: `pip install pywin32`. Not pinned here since it could not be installed/verified in
# this environment; verify it yourself on your target machine before relying on it in production.
# pywin32==308

# Cross-platform OS credential store (Windows Credential Manager / macOS Keychain /
# Linux Secret Service), for src/security/credential_store.py's Phase 16 Finding 2
# migration (docs/DECISIONS.md 2026-08-21) -- moves GEMINI_API_KEY out of plaintext
# .env storage. Unlike pywin32 above, this IS installable and testable in this
# project's Linux environment (verified: 19/19 tests passing here using keyring's
# backend-injection support), though this sandbox itself has no real backend
# configured (keyring.backends.fail.Keyring) -- credential_store.py detects that and
# falls back to .env/the environment automatically, same graceful-degradation
# pattern as at_rest.py. Real Windows Credential Manager behavior (via pywin32,
# which keyring pulls in automatically on Windows) still needs verification on
# real Windows hardware before relying on it in production.
keyring==25.5.0

