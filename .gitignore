# Build artifacts
build/
dist/
*.egg-info/
*.spec

# Installer output
installer/dist/
installer/staging/

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
*.egg

# Env / secrets — never commit real keys
.env

# Logs and local data (per Phase 8's retention model — these are runtime output, not source)
logs/
*.jsonl
episodic_memory.db
semantic_memory.db

# OS/editor cruft
.vscode/
Thumbs.db
# Chrome/Playwright profile data - contains real browser state, crash dumps,
# and potentially credential material (found leaking, 2026-08-12)
profiles/
profiles_broken_backup/
*.dmp
